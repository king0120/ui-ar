mutation inviteToAudition($auditionId: String!, $userId: String!, $timeSlotId: String, $projectId: String!) {
    inviteToAudition(auditionId: $auditionId, userId: $userId, timeSlotId: $timeSlotId, projectId: $projectId){
        id
    }
}
