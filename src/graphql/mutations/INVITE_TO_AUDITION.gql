mutation inviteToAudition($auditionId: String!, $userId: String, $users: [String!], $timeSlotId: String, $projectId: String!) {
    inviteToAudition(auditionId: $auditionId, userId: $userId, users: $users, timeSlotId: $timeSlotId, projectId: $projectId){
        id
    }
}
