query getAllRoles($projectId: String!){
    getAllRoles(projectId: $projectId) {
        id
        characterName
        characterSummary
        castTo {
            displayName
        }
        breakdown {
            ageRange
            gender
            unions
            ethnicity
            vocalRange
        }
    }
}
